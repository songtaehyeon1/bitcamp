<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.bitcamp.member.MemberDAOImp">
	<select id="loginCheck" parameterType="kr.co.bitcamp.member.MemberVO" resultType="kr.co.bitcamp.member.MemberVO">
		select userid, userpwd, username, userno from member where userid=#{userid} and userpwd=#{userpwd}
	</select>

	<select id="idFind" resultType="kr.co.bitcamp.member.MemberVO">
		select userid from member where username=#{param1} and useremail=#{param2}
	</select>
	
	<select id="pwdFind" resultType="kr.co.bitcamp.member.MemberVO">
		select userpwd from member where username=#{param1} and userid=#{param2} and useremail=#{param3}
	</select>

	<select id="idCheck" resultType="int">
		select count(userid) from member where userid=#{userid}
	</select>
	
	<insert id="memberInsert" parameterType="kr.co.bitcamp.member.MemberVO">
		insert into member(userno, userid, userpwd, username, useremail, usertel, userzipcode, useraddr, useraddrdetail, regdate) values(userno_sq.nextval, #{userid}, #{userpwd}, #{username}, #{useremail}, #{usertel}, #{userzipcode}, #{useraddr}, #{useraddrdetail}, sysdate)
	</insert>
	
	<select id="pwdCheck" resultType="int">
		select count(userpwd) from member where userid=#{param1} and userpwd=#{param2}
	</select>
	
	<select id="getMember" parameterType="string" resultType="kr.co.bitcamp.member.MemberVO">
		select userid, userpwd, username, useremail, usertel, userzipcode, useraddr, useraddrdetail from member where userid=#{userid}
	</select>
	
	<insert id="memberUpdate" parameterType="kr.co.bitcamp.member.MemberVO">
		update member set userpwd=#{userpwd}, useremail=#{useremail}, usertel=#{usertel}, userzipcode=#{userzipcode}, useraddr=#{useraddr}, useraddrdetail=#{useraddrdetail}
	</insert>
</mapper>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             

